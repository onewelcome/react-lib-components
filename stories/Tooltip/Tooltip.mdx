import { ArgTypes, Canvas, Meta, Story, Subtitle, Title } from "@storybook/blocks";
import { Icons, Icon } from "../../src/components/Icon/Icon.tsx";
import * as TooltipStories from "./Tooltip.stories.tsx";

<Meta of={TooltipStories} />
<Title />
<Subtitle />

The `Tooltip` component is used to give the end user information related to an element on the page. For instance, it might be used to give information about an input field in a form. It uses the [usePosition hook](/docs/hooks-useposition--page) under the hood.
In order to place the tooltip according to how you would like, you can use the `location` and `position` props. The `location` prop defines **where** the tooltip should be placed. The `position` prop defines **how** the tooltip should be placed. For instance, if you set `location="top"` and `position="center"`, the tooltip will be placed above the element, and centered horizontally. If you set `location="top"` and `position="left"`, the tooltip will be placed above the element, and aligned to the left of the element. The `location` prop can be one of `top`, `bottom`, `left`, or `right`. The `position` prop can be one of `start`, `center`, or `end`. The `start` position is the top or left side of the element, depending on the `location` prop. The `end` position is the bottom or right side of the element, depending on the `location` prop. The `center` position is the center of the element.

You can define a `label` prop which displays content, followed by an <Icon icon={Icons.InfoCircle} /> icon. The contents in between the `<Tooltip></Tooltip>` tags becomes the content of the popup that appears when you hover over the icon.

## Composability

The `label` prop accepts both strings and React nodes, providing flexibility in creating labels with rich formatting or complex structures. This allows for seamless integration of icons, styled text, or custom components within tooltip labels while maintaining design consistency.

## Tooltip properties

<table>
  <thead>
    <tr>
      <th>Property</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
      <th>Required</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>label</td>
      <td>ReactNode</td>
      <td></td>
      <td>
        The content that will be displayed next to the &lt;Icon icon=\{Icons.InfoCircle} /> icon.
        Can be a string or a React component.
      </td>
      <td>yes</td>
    </tr>
    <tr>
      <td>title</td>
      <td>string</td>
      <td></td>
      <td>The title that will be displayed in the tooltip popup.</td>
      <td>no</td>
    </tr>
    <tr>
      <td>children</td>
      <td>string</td>
      <td></td>
      <td>The content that will be displayed in the tooltip popup.</td>
      <td>yes</td>
    </tr>
    <tr>
      <td>location</td>
      <td>string</td>
      <td>top</td>
      <td>The location of the tooltip popup. Can be one of `top`, `bottom`, `left`, or `right`.</td>
      <td>no</td>
    </tr>
    <tr>
      <td>position</td>
      <td>string</td>
      <td>center</td>
      <td>The position of the tooltip popup. Can be one of `start`, `center`, or `end`.</td>
      <td>no</td>
    </tr>
  </tbody>
</table>

# Examples

Basic string label

```tsx
const StringLabelExample = () => (
  <Tooltip title="Example tooltip" label="This is a tooltip" location="top" position="center">
    This is the content that will be displayed when you hover over the icon.
  </Tooltip>
);
```

<Canvas>
  <Story of={TooltipStories.Tooltip} />
</Canvas>

React node as label

```
const ReactNodeLabelExample = () => (
  <Tooltip
    title="Formatted label example"
    label={<>Label with <strong>formatted</strong> text</>}
    location="right"
    position="center"
  >
    This tooltip demonstrates using a React node as a label.
  </Tooltip>
);
```

<Canvas>
  <Story of={TooltipStories.TooltipExamples} />
</Canvas>

# Props

<ArgTypes />
