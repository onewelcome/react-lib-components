import { DocsStory, Title, Subtitle, ArgsTable, PRIMARY_STORY } from "@storybook/addon-docs";

<Title />
<Subtitle />

The component library can be customised in a limited fashion. This customisation is done through the use of this `BaseStyling` component, which should be wrapped around your entire application. Using the `properties` prop, you can pass an object
with any of the properties you find in the [props table down below](#props). All of the colors and values you'll find for each of the properties are their default values. Meaning that if you just wrap your application in the `BaseStyling` component
without passing anything to the `properties` prop, these are the values you'll work with.

## colorFocus

Will get translated to the CSS variable `--color-focus`. By default this refers to the same color as `--color-primary`, so if you change the `--color-primary` variable to a different color, this property will automatically change as well.

## colorPrimary

Will get translated to the CSS variable `--color-primary`. This is the main color for the component library and is used everywhere. Every component uses this color in one way or another, so changing this variable to a color of your choice will
shape the entire look and feel of your application.

## colorSecondary

Will get translation to the CSS variable `--color-secondary`. This is a secondary color which is used only for the [Button component](/story/components-inputs-button--filled-button&args=color:secondary) and the [Link component](/story/components-navigation-link--link&args=color:secondary).
As you can see, `secondary` is one of the colors that you can select from the `color` prop.

## colorTertiary

Will get translation to the CSS variable `--color-tertiary`. This is a tertiary color which is used only for the [Button component](/story/components-inputs-button--filled-button&args=color:tertiary) and the [Link component](/story/components-navigation-link--link&args=color:tertiary).
As you can see, `tertiary` is one of the colors that you can select from the `color` prop.

## defaultLineHeight

Is the default line

    defaultLineHeight: "26px",
    buttonBorderRadius: "20px",
    buttonBorderWidth: "2px",
    buttonFontSize: "1rem",
    buttonBorderStyle: "solid",
    buttonFillTextColor: "#fff",
    buttonFillBackgroundColor: "#fff",
    buttonOutlineHoverTextColor: "#fff",
    inputBorderRadius: "8px",
    inputBorderWidth: "1px",
    inputBorderWidthFocus: "2px",
    inputBorderStyle: "solid",
    inputBackgroundColor: "#fff",
    modalShadowColor: "rgba(0, 0, 0, 0.16)",
    modalBackgroundColor: "#f5f8f8",
    modalHeaderBackgroundColor: "#fff",
    snackbarTextColor: "#fff",
    snackbarInfoBackgroundColor: "#003b5e",
    snackbarSuccessBackgroundColor: "#008a28",
    snackbarErrorBackgroundColor: "#d9291c",
    snackbarBorderRadius: "8px",
    dataGridRowBackgroundColor: "transparent",
    dataGridRowHoverBackgroundColor: "#f5e6f0",
    tabsBackgroundColor: "#FFF",
    tabBorderWidth: "2px",
    tabBorderStyle: "solid",
    tablistBorderWidth: "1px",
    tablistBorderStyle: "solid",
    tablistBorderColor: "#C3C3C7",
    tabTextColor: "#0f0f1e",
    default: "#0f0f1e",
    success: "#008a28",
    error: "#d9291c",
    disabled: "#e9e9eb",
    greyedOut: "#6f6f76",
    lightGreyBorder: "#e9e9eb",
    warning: "#ff6105",
    light: "#FFF",
    grey: "#c3c3c7",
    fontFamily: "'Red Hat Display', sans-serif",
    fontFamilyCode: "'Red Hat Mono', monospace",
    fontSize: "1rem",
    fontSizeH1: "2.5rem",
    fontSizeH2: "1.625rem",
    fontSizeH3: "1.5rem",
    fontSizeH4: "1.25rem",
    fontSizeSub: ".75rem",
    fontSizeCode: "1rem"

# Examples

<DocsStory id="components-layout-basestyling--base-styling" />

# Props

<ArgsTable story={PRIMARY_STORY} />
