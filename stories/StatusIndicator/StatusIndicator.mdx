import { ArgTypes, Canvas, Meta, Story, Subtitle, Title } from "@storybook/blocks";
import * as StatusIndicatorStories from "./StatusIndicator.stories.tsx";

<div style={{
    color: "red",
    fontSize: "2em",
    padding: "1em",
    border: "2px solid red",
    borderRadius: "5px",
    backgroundColor: "pink"
}}>

    **THIS COMPONENT HAS BEEN DEPRECATED IN FAVOR OF THE [TAG COMPONENT](?path=/docs/components-feedback-tag--docs)**

</div>
<br/>

<Meta of={StatusIndicatorStories} />
<Title />
<Subtitle />

`StatusIndicator` component is used to display a status badge along the status text.
It accepts a `status` prop, which can be used to change the badge's color and a `badgeClassName`, which can be used to style the status badge.
`StatusIndicator` uses a `Typography` component to display the status text - `typographyProps` prop can be used to adjust that component to your liking.

# Examples

<Canvas>
  <Story of={StatusIndicatorStories.StatusIndicator} />
</Canvas>

# Props

<ArgTypes />
