import { ArgsTable, PRIMARY_STORY, Subtitle, Title } from "@storybook/addon-docs";
import { Typography } from "../../src/Typography/Typography.tsx";

<Title />
<Subtitle />

The typography component helps present text content. It has styling defined for all kinds of uses. Text can be rendered within all valid HTML tags, regardless of the proper styling setting that should be used (e.g., text in div can be styled as an H1 element).

## Font

Typography was designed to support any font you like. This can be changed by the configuration provided to the `BaseStyling` component. By default `Roboto` font is set.
`Roboto` is not automatically loaded. You are responsible for including font in your application.

| NOTE: `code block` is the only exception that uses the font `Roboto Mono`. You only have to include it in your application if you use it. |
| ----------------------------------------------------------------------------------------------------------------------------------------- |

## Google fonts

You can take the HTML snippet beneath and put it in your application.

```html
<link
  href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,400;0,500;0,700;1,400;1,500;1,700&display=swap"
  rel="stylesheet"
/>
<link href="https://fonts.googleapis.com/css2?family=Roboto+Mono&display=swap" rel="stylesheet" />
```

## Install with npm

Alternatively, you can install font as a dependency using npm to get the benefits of self-hosting fonts. Read [more](https://www.npmjs.com/package/@fontsource/roboto).

```sh
npm i @fontsource/roboto @fontsource/roboto-mono
```

## Changing default font-size & font-family

`<BaseStyling/>` supports overwriting default properties. For Typography, these properties can be set:

| Property name  | Default value             | Description                                                              |
| -------------- | ------------------------- | ------------------------------------------------------------------------ |
| fontFamily     | Roboto, sans-serif        | Base font family used for all Typography's variants beside `code`        |
| fontFamilyCode | 'Roboto Mono', sans-serif | Font family used for `code` Typography variant                           |
| fontSize       | 1rem                      | Base font size used in Typography as well in other elements like buttons |
| fontSizeH1     | 2.5rem                    | Font size of Heading1 variant                                            |
| fontSizeH2     | 1.625rem                  | Font size of Heading2 variant                                            |
| fontSizeH3     | 1.5rem                    | Font size of Heading3 variant                                            |
| fontSizeH4     | 1.25rem                   | Font size of Heading4 variant                                            |
| fontSizeSub    | 0.75rem                   | Font size of Sub text variant                                            |
| fontSizeCode   | 1rem                      | Font size of Code variant                                                |

## Using spacing

By default all `Typography` elements have a `margin-bottom` property set to `1rem`. This can be changed by setting `spacing` property.

# Examples

```tsx
<Typography variant="h1">Heading 1</Typography>
```

You can see all the variants below:

<Typography variant="h1">Heading 1</Typography>
<Typography variant="h2">Heading 2</Typography>
<Typography variant="h3">Heading 3</Typography>
<Typography variant="h4">Heading 4</Typography>
<Typography variant="body">body</Typography>
<Typography variant="body-bold">body-bold</Typography>
<code>
  <Typography variant="code">code</Typography>
</code>
<br />
<Typography variant="sub-text">sub-text</Typography>

# Props

<ArgsTable story={PRIMARY_STORY} />
